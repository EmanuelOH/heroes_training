# --> name
spring.application.name=hero_training


# --> Configuration of the DB
spring.datasource.url=jdbc:mysql://localhost:3306/heroes_academy
spring.datasource.username=root
spring.datasource.password=Rlwl2023.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --> Configuration of JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --> Configuration of security
spring.security.user.name=admin
spring.security.user.password=admin123

# --> Enable Swagger (Springdoc OpenAPI)
springdoc.api-docs.path=/api/docs
springdoc.swagger-ui.path=/api/swagger-ui.html

# --> Configuration of Cloudinary (if necessary)
cloudinary.cloud.name=tu_cloud_name
cloudinary.api.key=tu_api_key
cloudinary.api.secret=tu_api_secret

# --> Configuration of JWT
jwt.secret=tu_jwt_secret
jwt.expiration=3600

# --> Other adjustments (if necessary)
spring.main.allow-bean-definition-overriding=true
